{
  "name": "dphvirtualauto",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "node scripts/generate-selectors-barrel.js",
    "test": "playwright test",
    "pretest:ui": "node scripts/generate-selectors-barrel.js",
    "test:ui": "playwright test --ui",
    "pretest:grep": "node scripts/generate-selectors-barrel.js",
    "test:grep": "playwright test --grep @critical",
    "pretest:chromium": "node scripts/generate-selectors-barrel.js",
    "test:chromium": "playwright test --project=chromium",
    "report:open": "playwright show-report",
    "allure:generate": "allure generate -c allure-results",
    "allure:open": "allure open",
    "prepare": "playwright install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.10.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.2",
    "dotenv": "^17.2.3",
    "playwright": "^1.56.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@slack/webhook": "^7.0.6"
  }
}
